/**
 * Copyright 2017 d-fens GmbH
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

using System.Diagnostics.CodeAnalysis;

namespace Net.Appclusive.Public.Constants
{
    [SuppressMessage("ReSharper", "InconsistentNaming")]
    public static class Approval
    {
        public const string PREFIX = "Net.Appclusive.Public.GlobalAttributes.Approval";
        private const string DELIMITER = ".";

        public static class Approval0
        {
            public const string INDEX = "0";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval1
        {
            public const string INDEX = "1";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval2
        {
            public const string INDEX = "2";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval3
        {
            public const string INDEX = "3";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval4
        {
            public const string INDEX = "4";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval5
        {
            public const string INDEX = "5";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval6
        {
            public const string INDEX = "6";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval7
        {
            public const string INDEX = "7";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval8
        {
            public const string INDEX = "8";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class Approval9
        {
            public const string INDEX = "9";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class ApprovalA
        {
            public const string INDEX = "A";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class ApprovalB
        {
            public const string INDEX = "B";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class ApprovalC
        {
            public const string INDEX = "C";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class ApprovalD
        {
            public const string INDEX = "D";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class ApprovalE
        {
            public const string INDEX = "E";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }

        public static class ApprovalF
        {
            public const string INDEX = "F";

            public const string RoleId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RoleId);
            public const string UserId = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(UserId);
            public const string Type = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(Type);
            public const string AbsoluteExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(AbsoluteExpiration);
            public const string RelativeExpiration = PREFIX + DELIMITER + INDEX + DELIMITER + nameof(RelativeExpiration);
        }
    }
}
